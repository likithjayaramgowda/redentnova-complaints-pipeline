# ---------- Google ----------
# The Google Form writes to some Google Sheet (linked by Google).
# We will APPEND normalized rows into a dedicated worksheet named "Complaints".
GSHEET_ID=
GSHEET_WORKSHEET=Complaints
GOOGLE_APPLICATION_CREDENTIALS=./secrets/gcp-sa.json

# ---------- Output ----------
BACKUP_DIR=backups
LOG_LEVEL=INFO

# ---------- SharePoint target (site-by-path) ----------
SP_HOSTNAME=tenant.sharepoint.com
SP_SITE_PATH=/sites/Quality
SP_FOLDER=Backups/Complaints

# ---------- Microsoft Graph (app-only client credentials) ----------
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# ---------- Optional Email via Graph sendMail ----------
# Requires Graph app permission Mail.Send and a sender mailbox.
MAIL_SENDER_UPN=sender.mailbox@company.com

# Default recipients for scheduled backups (comma-separated).
BACKUP_EMAIL_TO=admin1@company.com,admin2@company.com
